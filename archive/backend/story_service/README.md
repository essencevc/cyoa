# Introduction

This is a backend service for the story service that helps to generate stories.

it includes two services

- `story` - This is the main service that handles the generation of stories.
- `continuation` - This pre-generates the next set of choices for a given story.
